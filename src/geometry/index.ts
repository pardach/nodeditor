/**
 * هستهٔ هندسه ۲بعدی — فاز بعد: boolean با paper.js یا flatten-js
 * فعلاً transformهای ساده در نودها انجام می‌شود.
 */
export type { Path2D, Shape2D, PathCommand } from '../core/types/values.js';
